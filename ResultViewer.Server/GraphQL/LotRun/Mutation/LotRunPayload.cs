using ResultViewer.Server.Models;

namespace ResultViewer.Server.GraphQL.Mutation
{
    // Define a record LotRunPayload that includes a LotRun object
    public record LotRunPayload(LotRun LotRun);
}
